diff --git a/sound/drivers/dummy.c b/sound/drivers/dummy.c
index 4317677ba..a3f94e8db 100644
--- a/sound/drivers/dummy.c
+++ b/sound/drivers/dummy.c
@@ -32,7 +32,10 @@ MODULE_LICENSE("GPL");
 
 /* defaults */
 #define MAX_BUFFER_SIZE		(64*1024)
-#define MIN_PERIOD_SIZE		64
+/* cdj3k-emu: bumped from 64 B to 4096 B so JUCE's audio loop wakes at ~47 Hz
+ * instead of ~1500 Hz when the host audio path is disabled — keeps the host
+ * QEMU vCPU exits low. */
+#define MIN_PERIOD_SIZE		4096
 #define MAX_PERIOD_SIZE		MAX_BUFFER_SIZE
 #define USE_FORMATS 		(SNDRV_PCM_FMTBIT_U8 | SNDRV_PCM_FMTBIT_S16_LE)
 #define USE_RATE		SNDRV_PCM_RATE_CONTINUOUS | SNDRV_PCM_RATE_8000_48000
